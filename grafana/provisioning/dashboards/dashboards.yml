apiVersion: 1

providers:
  - name: 'anomaly-detection'
    orgId: 1
    folder: 'MLOps'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
